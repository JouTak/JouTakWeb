"""Core account extensions are managed from the canonical User admin card.

Session metadata and token mappings deliberately have no standalone admin
registration because their raw fields are security-sensitive implementation
details.
"""
